package K3D


class Camera(val windowWidth: Int = 640, val windowHeight: Int = 480) {

    init {
        println("Initializing K3D Camera, windowWidth: ${windowWidth}  windowHeight: ${windowHeight}")
    }

}
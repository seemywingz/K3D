plugins {
    id 'org.jetbrains.kotlin.konan' version '0.9.1'
}

konan.targets = [ 'macos_x64' ]

konanArtifacts {
    library('K3D') {
        srcFiles fileTree('src/lib/K3D')
    }

    program('Example') {
        libraries {
            artifact 'K3D'
        }
    }
}